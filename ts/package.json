{
  "name": "@deliyuxiang/srn-client",
  "version": "0.1.4",
  "description": "SRN protocol client — Ed25519 signing, PoW, and API client (TypeScript)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src/**/*.ts"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "better-sqlite3": "^11.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
